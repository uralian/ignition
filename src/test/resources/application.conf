ignition
{
	cassandra.test
	{
#		for embedded: host=localhost, port=9142, thrift-port=9175		
		embedded = true
#		otherwise specify them manually:
#		external.host = localhost
#		external.port = 9042
#		external.thrift-port = 9160 
	}
}